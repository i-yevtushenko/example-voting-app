# Homelab overlay. Merge with charts/voting-app/values.yaml.

db:
  enabled: true

redis:
  enabled: true

global:
  domains:
    vote: vote.k8s.local
    result: result.k8s.local
  db:
    enabled: true
  redis:
    enabled: true
  gateway:
    provider: traefik
    parentRefs:
      - group: gateway.networking.k8s.io
        kind: Gateway
        name: traefik
        namespace: traefik
        sectionName: websecure

vote:
  image:
    repository: ghcr.io/i-yevtushenko/example-voting-app/vote
    tag: "879e328"

result:
  image:
    repository: ghcr.io/i-yevtushenko/example-voting-app/result
    tag: "879e328"

worker:
  image:
    repository: ghcr.io/i-yevtushenko/example-voting-app/worker
    tag: "879e328"
